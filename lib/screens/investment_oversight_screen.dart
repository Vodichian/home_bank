import 'package:flutter/material.dart';

class InvestmentOversightScreen extends StatelessWidget {
  const InvestmentOversightScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Investment Oversight'),
      ),
      body: const Center(
        child: Text('Investment Oversight Screen - Placeholder'),
      ),
    );
  }
}
